@page
@model UHPostalService.Pages.Tracking.CheckoutModel

@{
    ViewData["Title"] = "Shipment Check Out";
}

<h1>Shipment Check Out</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="nextstop" class="control-label">Next Stop</label>
                <select asp-for="nextstop" class ="form-control" asp-items="ViewBag.NextStops">ab</select>
            </div>

            <div class="form-group">
                <input type="submit" value="Check Out" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="/Shipments/Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}